// import { createProxyMiddleware } from 'http-proxy-middleware'

// export default function (app) {
// 	app.use(
// 		'/api',
// 		createProxyMiddleware({
// 			target: 'http://localhost:8000', // This should match the backend service name (bookstore) and port in the Docker setup
// 			changeOrigin: true,
// 		})
// 	)
// }
